export enum RoleEnum {
    user = "user",
    admin = "admin",
    superAdmin = "superAdmin"
}

export enum GenderEnum {
    male = "male",
    female = "female",
    
}

export const ProviderEnum = {
    system:"system",
    google:"google"
}